// ? :
const age=4;
age>18? console.log("eligible to vote") : console.log("not eligible");




const marks=90;

const result=marks<40 ? "need to work hard" : marks<60 ? "B grade":marks<75 ? "a grade":marks<85 ? "A+ grade":"Genius";
console.log(result);